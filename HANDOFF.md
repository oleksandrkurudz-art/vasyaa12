# HANDOFF — передача контексту для наступного чату

> Стислий вступ. Деталі стека/структури — в `AGENTS.md`, повне ТЗ — `ТЗ.txt`,
> історія змін — у git. Тут лише поточний стан, відкриті задачі й неочевидні «граблі».

---

## 1. Що це

**НРГ — Незалежний регіональний голос** — регіональний новинний MVP-портал (до 2026-07
називався «Громада.Новини»). Головна фішка — **контекстна реклама**
біля статей (рушій `src/lib/ads.ts`). Деталі — в `AGENTS.md`.

- **Живий сайт:** https://vasyaa12.vercel.app (Vercel-проєкт `vasyaa12`).
- **Репо:** https://github.com/oleksandrkurudz-art/vasyaa12 (гілка `main`).
- БД — **Supabase Postgres** (ref `pruswnyuxxaqmxrchhzu`, eu-west-3), підключення **«Transaction pooler»** (порт `6543`) — правильний режим для serverless/Vercel.

---

## 2. Поточний стан

Повний MVP задеплоєно й працює end-to-end (новина в адмінці → сайт → контекстна реклама).
Дизайн — у стилі ТСН. Базові блоки: SEO/OG+JSON-LD; живі курси НБУ (`src/lib/rates.ts`) +
погода open-meteo (`src/lib/weather.ts`); фото обкладинок у Supabase Storage зі стисненням.

### ⭐ Велике бачення клієнта (2026-06-18)
Портал має стати **«цифровим центром життя громад Калущини»** (не просто новини). Повний
список і пріоритети — у файлі пам'яті `vasya-vision-roadmap.md`. Головна модель грошей —
**бізнес-картки** (сторінки підприємств, ~100-300 грн/міс), НЕ банери. Рішення: **мультигромадність**
від початку; найближча мета — **трафік**.

### ✅ Зроблено цією сесією (2026-06-18, ВСЕ ЗАДЕПЛОЄНО)
- **Громади:** таблиця `Community` (5 громад Калущини, id 1-5), `Article.communityId`
  (nullable = «весь район»). Вибір громади у формі статті. **Перемикач громади у шапці**
  (`CommunitySwitcher.tsx`, cookie `community`) — стрічка/розділ/популярне фільтруються:
  громада + загальнорайонні (`src/lib/community-filter.ts`).
- **Розділів тепер 9:** додано `lyudy` «Люди громади» (sky), `nekrolohy` «Некрологи» (slate).
- **Червона стрічка зверху (`BreakingBar.tsx`):** (1) АВТО — повітряна тривога в Івано-Франківській
  обл. (`src/lib/airraid.ts`, безкоштовне API UBilling, кеш 60с); (2) інакше — ручна «термінова»
  новина (галочка `breaking` у формі). Тривога перебиває ручну.
- **Головна перебудована:** після героя — `NewsTicker.tsx` «Стрічка новин» (текст, без фото).
  Телефон: лише стрічка. ПК: ліворуч «Головні події» (картки з фото, `hidden lg:block`),
  праворуч (340px) стрічка + Sidebar (`lg:grid-cols-[1fr_340px]` + `lg:order-1/2`).

### ✅ Зроблено (2026-07-24, каталог бізнесу — ЗАДЕПЛОЄНО через прямий SQL)
- **Об'єднано `Advertiser` → `Business`** (єдина модель): картка в каталозі + власник
  банерів. `Ad.advertiserId` → `Ad.businessId` (обов'язковий, `onDelete: Cascade`),
  `Ad.linkUrl` тепер опційний (`@default("")`). Демо-рекламу прибрано, налито 8 демо-бізнесів.
- **Каталог `/kataloh`** + деталка `/kataloh/[slug]` (JSON-LD LocalBusiness): фільтр за
  категорією (`?kat=`, список `src/lib/business-categories.ts`) + громадою (cookie).
  Дата-шар — `src/lib/businesses.ts`. Правило видимості: `active && (paidUntil null || >= now)`
  — прострочені/вимкнені ховаються з сайту, підсвічуються в адмінці (червоне «прострочено»).
- **Банер веде на картку**: порожній `linkUrl` → внутрішній `<Link>` на `/kataloh/{slug}`;
  зовнішній URL → `<a target=_blank sponsored>`. Рушій (`ads.ts`) не показує банер невидимого бізнесу.
- **Адмінка**: `/admin/businesses` (CRUD, `BusinessForm`) замість «Рекламодавців»; AdForm —
  завантаження банера через `CoverUpload` (узагальнено пропсами) + select бізнесу.
- ⚠️ **Міграцію застосовано ПРЯМИМ SQL у транзакції** (`pg`, через `DATABASE_URL`), бо Supabase
  MCP був відключений. Prisma 7: `migrate diff --from-config-datasource --to-schema … --script`
  (прапорець `--from-url` прибрано). Папка `migrations/` як і раніше init-only.
- ⚠️ **`db:seed` НЕ ЗАПУСКАТИ проти прод** (зітре статті) — демо-бізнеси лили окремим
  одноразовим ідемпотентним скриптом. `seed.ts` оновлено для чистих середовищ.

### Відкриті задачі / наступні кроки (за пріоритетом цінності)
- **Оплата бізнес-карток**: зараз `paidUntil` виставляється вручну в адмінці (готівка/переказ).
  Далі — за потреби онлайн-оплата.
- **Telegram-бот** для AI-копірайтингу новин — ✅ КОД НАПИСАНО (вебхук `src/app/api/telegram/route.ts`,
  `copywriter.ts`/`extract.ts`/`telegram.ts`/`article-write.ts`). Провайдер — **OpenAI** (`openai` SDK,
  json_schema strict; модель `OPENAI_MODEL`, дефолт `gpt-4o-mini`) — НЕ Claude (вибір клієнта).
  Кидаєш текст/лінк/фото-з-підписом → чернетка → прев'ю з кнопками Опублікувати/Чернетка/Видалити.
  Лишилось: задати env у Vercel + локально (`OPENAI_API_KEY`, `TELEGRAM_BOT_TOKEN`,
  `TELEGRAM_WEBHOOK_SECRET`, `TELEGRAM_ALLOWED_IDS`), задеплоїти, один раз
  `npx tsx scripts/set-telegram-webhook.ts <prod-url>`.
- Інше з бачення: онлайн-довідник, push, кабінет мешканця, Facebook-автопостинг, власний домен;
  пізніше/дорого — відео, трансляції, мобільний додаток, карта, Work.ua.
- ✅ GitHub-автодеплой під'єднано (2026-07) — `push` у `main` деплоїть прод сам (див. розділ 3).

---

## 3. Критичні «граблі» (коштували дебагу, в коді неочевидно)

- **Vercel ріже тіло запиту ~4.5 МБ** → фото з телефона (5-8 МБ) падало з **413** ще до сервера.
  Тому стиснення **у браузері** перед відправкою (`src/components/admin/CoverUpload.tsx`,
  canvas→WebP, 2048px/q0.9). `serverActions.bodySizeLimit` тут НЕ допомагає.
- **`sharp` — нативний модуль**, Vercel не клав його в бандл → 500. Фікс у `next.config`:
  `outputFileTracingIncludes: { "/admin/**": ["node_modules/sharp/**/*", "node_modules/@img/**/*"] }`.
  Сервер чіпає лише сирий JPEG/PNG; WebP від браузера **не перекодовує** (інакше подвійне стиснення).
- **Стейл Prisma-клієнт** мовчки давав 404 на розділах/статтях. Фікс: скрипт `predev` →
  `prisma generate` на кожному `npm run dev`. Запускаєш dev інакше — зроби `npx prisma generate` сам.
- **Пароль БД містить `@`** → у `DATABASE_URL` кодувати як `%40`.
- **Пул з'єднань: Transaction pooler (порт 6543), НЕ Session pooler (5432).** Session-режим тримав
  з'єднання на весь час сесії → локальний dev + прод-лямбди перебивали ліміт (~15) і давали
  `EMAXCONNSESSION`. Transaction pooler мультиплексує короткі з'єднання. Адаптер `PrismaPg` за
  замовчуванням НЕ кешує prepared statements, тож із transaction-режимом сумісний без `pgbouncer=true`.
  Перехід = лише порт `5432→6543` у `DATABASE_URL` (хост/юзер/пароль ті самі) локально **і** у Vercel env.
- **Деплой автоматичний з GitHub** (Git-інтеграція Vercel, підключена 2026-07). `git push` у `main`
  → прод-деплой сам, ~40с. ⚠️ **Push = публікація на живий сайт**, окремої команди деплою не треба.
  CLI (`vercel deploy --prod --yes`) не потрібен; так деплоїли до липня 2026, звідси старі
  деплої з міткою `gitDirty`. Env і далі через CLI/дашборд:
  `printf '%s' '<val>' | vercel env add <NAME> production`.
- **Схемою керуємо через Supabase MCP** (`apply_migration`/`execute_sql`), build НЕ робить
  `migrate deploy`. Нова міграція: `npx prisma migrate diff … --script` → застосувати через MCP.
  MCP не віддає пароль БД і service-role ключ — їх дає користувач.
- **`preview_screenshot` зависає** (30s) на завантаженні фото з picsum. Перевіряй через
  `preview_eval`/`preview_inspect` (DOM, computed styles, статуси), не скріншотами.
- **Не чисти `.next` / не запускай прод-білд, поки крутиться dev** — ENOENT/MODULE_NOT_FOUND.
- **API тривог (`airraid.ts`) жорстко лімітує** (429 при частих запитах) — тому власний кеш 60с;
  область зашита як `AIR_RAID_REGION = "Івано-Франківська область"` (точна назва-ключ з API).

### Env (Vercel Production + локальний `.env` у gitignore)
`DATABASE_URL`, `ADMIN_PASSWORD`, `SESSION_SECRET`, `SUPABASE_URL`,
`SUPABASE_SERVICE_ROLE_KEY` (формат `sb_secret_…`, лише сервер). У репо секретів немає.
Локальний `npm run dev` ходить у ту саму прод-базу Supabase.

---

## 4. Дизайн (вектор + ключові рішення)

Шапка (`Header.tsx`) — **чорний масthead у стилі BBC**, тонкий, не липкий на ПК:
сітка 3 колонки `[пошук] · [НРГ по центру] · [громада]` (дата й курси прибрані з шапки —
курси переїхали в сайдбар до погоди). Нижче — **окрема біла липка смуга навігації**
(`NavBar.tsx` → клієнтський `StickyNav.tsx`, `neutral-100`), розділи **відцентровані**;
на скролі вона стискається в компактний BBC-бар `[≡🔍 · НРГ · громада]`, що зʼїжджає згори
(fixed + translateY), а розділи ховаються під одну іконку «меню+пошук». Герой (`HeroBlock.tsx`)
— full-bleed фонове фото головної новини. Палітра: `brand-600 = #2563eb` (синій акцент),
`brand-950 = #122443` (запасний темно-синій), `urgent-600` — червоний (термінові). Шрифти: **Inter** (текст)
+ **Unbounded** (`font-display`, заголовки/логотип; повна кирилиця, через `next/font/google`).
⚠️ Не називай токен `@theme` як `--font-display` — він конфліктує з кастомним класом `.font-display`
(заголовки тоді мовчки лишаються Inter). Токен лишай під іншим імʼям; клас бере `var(--font-unbounded)`.

### ⚠️ Ширина (важливо для нових публічних сторінок!)
`<main>` у `(public)/layout.tsx` **не має `max-w-7xl`** (щоб герой був full-bleed без `100vw`,
який давав горизонтальний скрол). **Кожна публічна сторінка сама обгортає контент** у
`<div className="mx-auto w-full max-w-7xl px-4 py-6">`.

### Дрібниці, які легко зламати
- Перегляди показуються **лише коли > 100** (хелпер `hasEnoughViews` у `format.ts`).
- Колірні класи розділів (9 шт.) — **літеральні Tailwind-рядки** в `CATEGORY_STYLES` (щоб сканер їх згенерував).
- **Колір-стратегія (маркетинг): бренд = ОДИН синій.** `solid`/`onDark`/`text`/`border` у `CATEGORY_STYLES` однакові сині для всіх розділів (спільний `BRAND`); колір розділу лишається ЛИШЕ в дрібному `badge`-чипі (тиха мітка для сканування). Не повертай кольорові герой-бейджі/бордюри розділів — це вбиває впізнаваність бренду й «топить» рекламу/CTA.
- **«Терміново» ПОВЕРНУЛИ** (`BreakingBar.tsx`): авто-тривога (пріоритет) або ручна `breaking`-новина.
- Громади: новина з `communityId = null` — загальнорайонна (видно в усіх громадах); вибір громади в cookie `community`.
- Головна: текстова `NewsTicker` (ліворуч на ПК — «Головні події» з фото, `hidden lg:block`).
- **Категорії й громади живуть у БД** — нову треба додати І в `categories.ts`/`communities.ts`, І в БД (через Supabase MCP).
- **Навігація — priority+ (`NavLinks.tsx`, відцентрована):** скільки розділів влазить, решта — під «Ще ▾» (перерахунок через ResizeObserver + `window.resize` з rAF, заміри з прихованого списку). «Головна» і «Каталог бізнесу» — фіксовані видимі пункти. ⚠️ Якщо міняєш CSS-`gap` меню — онови константу `GAP` угорі файлу (зараз `gap-1`=4px), інакше overflow-розрахунок з'їде.
- **`StickyNav.tsx` (клієнт) — два стани білої смуги:** верх = повний `NavLinks`; скрол > 64px = компактний бар `[≡🔍 · НРГ · громада]` (fixed, зʼїжджає translateY+fade). Одна іконка «≡🔍» відкриває панель: **поле пошуку зверху** + усі розділи (бургер). Громаду тримає окремий `CommunitySwitcher variant="light"` праворуч. Дані про громади тягне `NavBar.tsx` (server) і передає в StickyNav.
- **Хедер (`Header.tsx`) — 3 зони масthead-а:** пошук (ліворуч, `HeaderSearch`) → **НРГ по центру** (grid `[1fr_auto_1fr]`) → громада (праворуч) + бургер на телефоні. Дата й курси **прибрані** з шапки; курси — плашка «Курс валют · НБУ» у `Sidebar.tsx` під погодою (`formatRateUk`).

---

### ⭐ Ребрендинг НРГ (2026-07-29)
**«Громада.Новини» → «НРГ — Незалежний регіональний голос»** (повна заміна, стара назва ніде
не лишається). Назва/слоган — `SITE_NAME`/`SITE_SLOGAN` у `src/lib/categories.ts` (єдине джерело:
метадані, OG, підвал, адмінка, промпт копірайтера). Знак — `src/components/Logo.tsx`: **три
квадратні плашки «Н» «Р» «Г» у стилі BBC**. Пропи: `size` (`sm`/`md`) + `tone`:
- `tone="onDark"` (шапка) — білі плашки, чорні літери. На головному хедері (`md`) плашки
  40×40px, більший відступ між ними (`gap-2`), кегль `text-[1.35rem]`.
- `tone="onLight"` (типово: підвал + компактний бар) — чорні плашки, білі літери (як у BBC).
⚠️ Рішення клієнта (кілька ітерацій, НЕ повертай): **без бейджа «LIVE»**, **без синьої плашки/
градієнта** в лого («виглядало дешево» — здешевлював саме колір, тому знак тепер моно-плашки).
Фавікони `src/app/icon.svg` + `icon.png` (перегенеровано зі SVG через `sharp`; ⚠️ ще стара
іконка-крапка — під новий boxed-знак НЕ перегенеровано, спитати за потреби) і `public/placeholder.svg`.

### Навігація (оновлено 2026-07-30)
Дворівнева, редакторська, **відцентрована** у білій смузі: **Головна · Каталог бізнесу ·
Новини · Влада · Події · Люди · Оголошення · Ще ▾**. «Каталог бізнесу» — тепер **звичайний
текстовий пункт** (одразу після «Головної»), а не контурна пігулка; лінкує на `/kataloh`.
Новинну рубрику **«Бізнес» перенесено під «Ще»** (`group:"more"` у `CATEGORIES`) разом із
Фотогалерея/Вакансії/**Пам'ять**. «Головна» і «Каталог» — фіксовані видимі, priority+ overflow.
**Назви розділів керуються кодом**: усюди `categoryName(slug)` замість `category.name` (у БД
`name`/`order` синхронізовано через Supabase MCP). ⚠️ Slug-и НЕ мінялись (`/polityka` = «Влада»,
`/biznes` = «Бізнес») — читабельні адреси лишились на потім (потребують редиректів).

_Стиль роботи з користувачем (українською, продуктове мислення, економія токенів) — у файлах
пам'яті (`working-style-vasya`, `vasya-vision-roadmap`). Інвентар компонентів — у `src/components/`.
Останнє оновлення: 2026-07-30 (BBC-масthead: центроване НРГ-плашками, компактний бар на скролі
`[≡🔍·НРГ·громада]`, курси → сайдбар, «Каталог» як пункт меню, «Бізнес» під «Ще» — усе задеплоєно)._
